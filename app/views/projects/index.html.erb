<div class="container">
  <h1>Projects</h1>
  <% @projects.each do |p| %>
    <div class="project">
      <h3><%= link_to p.title, project_path(p) %></h3>
      <p><%= truncate(p.description, length: 200) %></p>
    </div>
  <% end %>
</div>
